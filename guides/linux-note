echo $XDG_SESSION_TYPE       # check X11 or wayland
sudo fwupdmgr update              # check system boottime
systemd-analyze time                # display information of the system
uname -a                            # for all the stuff
alias ls="ls --color=auto"          # link bash cmd with config
mkdir -p {}                       # create multiple directory within {}
sudo systemctl enable --now [service] # enable a service
sudo systemctl status [service] # check service status
head and tail -n 5
journalctl
touch [1..3].txt
ln -s create link
find -name -type
finger
tac and cat 
# same but reverse order of display
ncdu # gui disk analyzer
route -n # show IP only
source bash script for it to take affect
chmod u+s file      # Set SUID
chmod g+s directory # Set SGID
chmod +t directory  # Set sticky bit

java -jar #run file
bat # a better cat cmd

prime-run %command$ #For steam launch options using nvidia gpu

Use Ctrl + Shift + T for undo closed tab | Ctrl + Alt + V/H for split tabs (Zen Browser)
dd
git install
gcc long cmd
fzf
pacman -Rns

SQL CMD
Some commands to remember when starting out SQL:

CREATE DATABASE my_new_database;
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100)
);
sudo mysql -u root -p
GRANT ALL PRIVILEGES ON *.* TO 'root'@'localhost' IDENTIFIED BY 'your_password';
PRIVILEGES
SELECT user, host FROM mysql.user;

GRANT ALL PRIVILEGES ON database_name.* TO 'new_user'@'localhost';
SHOW GRANTS FOR 'new_user'@'localhost';
For Linux Mint ppa problem:
wget http://archive.ubuntu.com/ubuntu/pool/universe/liba/libaio/libaio1_0.3.112-5_amd64.deb

sudo pacman -S libheif (to read heic file)
